name: C/C++ CI

on:
  push:
    branches: [ all ]
  pull_request:
    branches: [ all ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: make
      run: make
